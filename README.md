# ext-renewon

Public contract repository for RenewOn. `frontend/` is the sole publisher of
`@sneat/extension-renewon-contract`; the paired `renewon` repository owns the
runtime package and standalone application.
